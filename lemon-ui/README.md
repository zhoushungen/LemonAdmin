# lemon-ui

Vue 3 + TypeScript 管理端。通用列表能力集中在 `src/components/lemon`：全局字号与配色、列头拖动、字段显示、当前字段导出、CSV 导入预览。

```bash
npm install
npm run dev
npm run build
```

API 默认代理到 `http://localhost:5080`，可通过 `VITE_API_BASE_URL` 调整。
